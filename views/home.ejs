<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <style>
     .header-container {
      display: flex;
      align-items: center; 
      margin-bottom: 20px;
    }

    .header-container h2 {
      margin-right: 20px; 
    }

    .card {
      box-shadow: 0 4px 8px 0 rgba(0,0,0,0.2);
      transition: 0.3s;
      width: 400px;
      height: 500px;
      margin: 20px auto
    }
    
    .card:hover {
      box-shadow: 0 8px 16px 0 rgba(0,0,0,0.2);
    }
    
    .container {
      padding: 2px 16px;
    }

    .description {
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: normal;
      max-height: 100;
      max-height: 3.6em; 
      margin-bottom: 8px;
    }
    .card-container {
      display: flex;
      flex-wrap: wrap;
      max-height: 80vh;
      overflow-y: auto;
      justify-content: center;
      align-items: flex-start;
    }

    .search-container {
      display: grid;
      grid-template-columns: 2fr 1fr auto; 
      gap: 10px; 
      align-items: stretch;
    }

    .search-container input,
    .search-container input[type="submit"] {
      width: 100%;
      padding: 8px;
      box-sizing: border-box;
      height: 60%; 
    }
    </style>
</head>
<body>
  <div class="header-container">
    <h2>Arts Collection</h2>
    <a href="/arts/add">Add Art</a>
  </div>


  <form action="/" method="get" class="search-container">
    <input type="text" autocomplete="off" name="searchTitle" placeholder="Search by title">
    <input type="text" autocomplete="off" name="searchArtist" placeholder="or by artist">
    <input type="submit" value = "Search" style="margin-bottom: 20px;">
  </form>

  <%=// JSON.stringify(summarize[0]) %>

  <div style="background-color:blanchedalmond;">
    <p>Jumlah seluruh art adalah <%=summarize[0].dataValues.total%>. Untuk art tertua yaitu berasal dari tahun <%=summarize[0].dataValues.min%>, serta untuk yang terbaru dari tahun <%=summarize[0].dataValues.max%> </p>
  </div>

<div class="card-container">
  <% data.forEach(el => { %>
  <div class="card" style="margin: 20px;">
    <img src="<%= el.photo %>" alt="" style="width:100%">
    <div class="container">
      <h4><b><%= el.name %></b></h4> 
      <p class="description"><%= el.description %></p> 
        <button><a href="/arts/<%= el.id %>">See Detail</a></button>
        <button><a href="/arts/<%= el.id %>/delete">Delete</a></button>
    </div>
  </div>
  <% }) %>
</div>

</body>
</html>